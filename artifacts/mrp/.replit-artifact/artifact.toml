kind = "web"
previewPath = "/"
title = "Steel Fabrication MRP"
version = "1.0.0"
id = "artifacts/mrp"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 19694

[services.development]
run = "pnpm --filter @workspace/mrp run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/mrp", "run", "build" ]
publicDir = "artifacts/mrp/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "19694"
BASE_PATH = "/"
